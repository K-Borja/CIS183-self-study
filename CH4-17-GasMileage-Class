/*4.17 (Gas Mileage) Drivers are concerned with the mileage their automobiles get. One driver has
kept track of several trips by recording the miles driven and gallons used for each tankful. Develop
a Java application that will input the miles driven and gallons used (both as integers) for each trip.
The program should calculate and display the miles per gallon obtained for each trip and print the
combined miles per gallon obtained for all trips up to this point. All averaging calculations should
produce floating-point results. Use class Scanner and sentinel-controlled repetition to obtain the
data from the user.*/

public class Trip
{
  private int miles;
  private int gallons;
  private int numOfTrips;
  private double milesPerGallon;
  private double total = 0;
  private double average = 0;
  
  //constructor
  public Trip()
  {
  }
  
  public Trip(int milesDriven, int gallonsUsed)
  {
    miles = milesDriven;
    gallons = gallonsUsed;
  }
  
  //set method
  public void setMiles(int milesDriven)
  {
      //set by user input
      miles = milesDriven;
  }
  public void setGallons(int gallonsUsed)
  {
      //set by user input
      gallons = gallonsDriven;
  }
  public void setNumberOfTrips(int numberOfTrips)
  {
      //set by a counter, passed to this method.
      numOfTrips = numberOfTrips;
  }
  
  //get methods
  public int getMiles()
  {
      return miles;
  }
  public int getGallons()
  {
      return gallons;
  }
  public int getNumOfTrips()
  {
      return numOfTrips;
  }
  
  public double milesPerGallonEachTrip()
  {
      milesPerGallon = miles / gallons;
      System.out.printf("\n\nFor this trip:\nMiles driven: %d\nGallons used: %d\nMiles Per Gallon:%d\n\n",getMiles(),getGallons(),milesPerGallon);
      return milesPerGallon;
  }
  
  public double totalMilesPerGallon()
  {
     total += milesPerGallon;
     return total;
  }
  
  public double averageMilesPerGallon()
  {
      average = total/numOfTrips;
      return average;
  }
  
  public void displayMessage()
  {
      System.out.printf("Total miles per gallon: %d\n", total);
      System.out.printf("Total number of trips: %d\n", getNumOfTrips());
      System.out.printf("Average miles per gallon: %d\n", average);
  }  
}
